// Mirror of web supabaseClient — configure with Expo env + AsyncStorage.
// import { createClient } from '@supabase/supabase-js';
// export const supabase = createClient(URL, ANON_KEY, { auth: { storage: AsyncStorage } });
export {}; // placeholder until Expo is initialised
